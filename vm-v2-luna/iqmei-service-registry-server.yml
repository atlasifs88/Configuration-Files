spring:
  application:
    name: iqmei-service-registry-server
  zipkin:
    base-url:  http://localhost:10000/
  sleuth:
    sampler:
      probability: 1  
  
    

    
logging.level.org.springframework.web.servlet.DispatcherServlet: DEBUG
    
server:
  port: 4001
  
eureka:
  instance:
    hostname: dev-config.atlasifs.com
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://dev-config.atlasifs.com:4001/}
